[pytest]
markers =
    prod: marker for prod tests

console_output_style = count

addopts =
    -v
    --qase-testops-api-token="token"
    --qase-mode="testops"
    --qase-testops-project="XTWP"
    --qase-testops-bulk=False
    --tracing retain-on-failure